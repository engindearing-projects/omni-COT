android.useAndroidX=true
org.gradle.jvmargs=-Xms256m -Xmx4096m
# Note: AGP 8.1+ removed `android.bundle.enableUncompressedNativeLibs`
# (was in the ATAK 5.6 docs). Modern equivalent is `jniLibs.useLegacyPackaging`
# in app/build.gradle's `packagingOptions { jniLibs { ... } }`, which is
# already set to `true` for our ATAK plugin.
